# Frontend configuration (Vite). Copy to `.env` to override.
# Base URL for the backend API. In dev the Vite proxy maps "/api" -> backend,
# so the default usually just works. Set an absolute URL only when the API runs
# on a different host. NEVER put API keys here — keys stay on the backend.
VITE_API_BASE_URL=/api
